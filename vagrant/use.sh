#!/bin/bash

echo "Just cat this cp.sh file."

# this is for private vm config
# mkdir a directory
# cp Vagrantfile to that directory and edit vm name, ssh config
# run $ vagrant up
# run $ vagrant ssh
# if ssh can't connect then enable gui and login edit network and retry ssh
